# Production Environment Variables for Railway Backend
# 
# IMPORTANT: These variables must be set in your Railway dashboard
# Go to: Railway Dashboard → Your Project → Variables
# 
# After adding/updating variables, redeploy your backend

# Frontend URL (Netlify deployment)
FRONTEND_URL=https://tka-ardhi.netlify.app

# Node environment
NODE_ENV=production

# Port (Railway auto-assigns this, but you can override if needed)
# PORT=5000

# Add your Supabase credentials here as well if not already set:
# SUPABASE_URL=your_supabase_url
# SUPABASE_ANON_KEY=your_supabase_anon_key
